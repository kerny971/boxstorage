import { PseudoValidatorDirective } from './pseudo-validator.directive';

describe('PseudoValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new PseudoValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
